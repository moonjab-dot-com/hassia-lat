# Full stack for local training/export. Not installed on the deploy
# target — see requirements.txt for the lean runtime deps actually
# deployed (TFLite-only, no full TensorFlow).
flask>=3.0
werkzeug>=3.0
pillow>=10.0
numpy>=1.26
opencv-python-headless>=4.9
tensorflow>=2.15
scikit-learn>=1.3
matplotlib>=3.8
