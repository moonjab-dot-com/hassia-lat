# Lean runtime deps for the deployed Flask app only.
# Full TensorFlow is NOT needed here — the app serves a TFLite model,
# which avoids the memory footprint that was getting OOM-killed on
# Render's free tier (512MB RAM). For training, see requirements-training.txt.
flask>=3.0
werkzeug>=3.0
gunicorn>=21.2
pillow>=10.0
numpy<2.0
opencv-python-headless>=4.9
tflite-runtime>=2.13,<2.15
